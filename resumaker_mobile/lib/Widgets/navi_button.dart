

import 'package:flutter/material.dart';

class NaviButton {

  Widget build()
  {
    return ElevatedButton(
      // Set a call to the register form screen.
      onPressed: () {
        // Navigator.pushNamed(context, '/register');
        
      },

    )
  }
}